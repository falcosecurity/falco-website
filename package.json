{
  "private": true,
  "scripts": {
    "postinstall": "git submodule update -f --init --recursive && cd themes/docsy && npm install"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "lodash": ">=4.17.21",
    "postcss": ">=8.4.21",
    "postcss-cli": "^10.1.0"
  }
}
